export class NewNoteInfo {
    offerId: string;    // The unique identifier of the offer that this note is associated with
    noteText: string;   // Text string of the note to be associated with the offer    
}
